[project]
name = "voice-agi-server"
version = "0.1.0"
description = "Voice AGI FastAPI (tasks + events websocket)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.0",
]
